import { NumpadKey } from '@Enums/numpad-key.enum.ts';

export enum Operator {
  Add = NumpadKey.Add,
  Multiply = NumpadKey.Multiply,
  Substract = NumpadKey.Substract,
  Divide = NumpadKey.Divide,
}
