import { FormatOperationPipe } from './format-operation.pipe';

describe('FormatOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
